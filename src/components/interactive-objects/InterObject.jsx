import { ReactComponent as Key } from "../../assets/key-solid.svg";

// ? Plan is to eventually make a component or higher order component for objects that the player can interact with.
// ? Many aspects of the item will be very similar so having one component or higher order component to reduce duplication of code would be useful

export const InterObject = () => (
  <div>
    <Key />
  </div>
);

export default InterObject;
